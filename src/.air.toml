root = "."
tmp_dir = "tmp"
[build]
  cmd = "cd ./cmd/web && go build -o ../../bin/main ."
  bin = "./bin/main"
  delay = 1000 # ms
  exclude_dir = ["assets", "tmp", "vendor"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_regex = ["_test\\.go"]
